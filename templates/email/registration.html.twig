{{ 'email.registration'|trans({
    '%confirm-link%': user.fullName,
    '%login-link%': url('security_confirm', { 'token': user.confirmationToken}),
    '%login-link%': url('security_login')
})|raw }}